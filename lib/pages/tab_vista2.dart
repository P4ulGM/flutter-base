import 'package:flutter/material.dart';

class TabVista2 extends StatelessWidget {
  const TabVista2({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Vista 2',
        style: TextStyle(fontSize: 40),
      ),
    );
  }
}